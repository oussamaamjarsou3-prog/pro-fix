/**
 * CarSpecio — Shared build configuration
 * Single source of truth for canonical domain and other build constants.
 */

module.exports = {
    BASE_URL: 'https://carspecio.com',
    SUPPORTED_LANGS: ['es', 'en', 'fr', 'ar'],
    DEFAULT_LANG: 'es',
    GUIDES_PATH: '/guias/',
    CARS_PATH: '/'
};
